# Dépendances backend BOOKTIME
# Contraintes en planchers (>=) plutôt que pins exacts : robustesse multi-Python (3.11 -> 3.14).
# Les pins exacts (==) sur numpy/pandas/scipy bloquaient l'installation sous Python 3.14
# (pas de wheel). Les bornes hautes ne sont posées que pour éviter une montée de majeure cassante.

# --- Web / API ---
fastapi>=0.115.0
starlette>=0.46.0
anyio>=4.9.0
uvicorn>=0.22.0
python-multipart>=0.0.6

# --- Auth / sécurité ---
python-jose[cryptography]>=3.3.0
PyJWT>=2.8.0
bcrypt>=4.1.2
email-validator>=2.1.0

# --- Base de données ---
pymongo>=4.6.0,<5.0.0

# --- HTTP clients ---
requests>=2.31.0
httpx>=0.27.2,<0.29
httpcore>=1.0.0,<2.0.0
aiohttp>=3.10.0

# --- Données / fichiers ---
pandas>=2.2.0
openpyxl>=3.1.5
Pillow>=10.4.0

# --- Utilitaires ---
python-dotenv>=1.0.0
deep-translator>=1.11.4
beautifulsoup4>=4.12.2
pydantic>=2.11.0
redis>=6.2.0

# --- Planification (rafraîchissement "prochaines sorties") ---
APScheduler>=3.10.0,<4.0.0

# --- Machine Learning (recommandations Phase 3.4) ---
# numpy>=1.26 : pip choisit 2.x sous Python >=3.13, 1.26.x sous Python plus ancien.
scikit-learn>=1.5.0
numpy>=1.26.0
scipy>=1.13.0
joblib>=1.4.0
threadpoolctl>=3.5.0

# --- Tests (Phase 4.1) ---
pytest>=7.4.3
pytest-asyncio>=0.24.0,<0.27
pytest-mock>=3.12.0
pytest-cov>=4.1.0
factory-boy>=3.3.0
faker>=20.1.0
