# Surcharges locales (copier en .env.local — gitignored)
# Prioritaire sur .env pour tester sans toucher Mongo Atlas prod.

RAILWAY_MONGODB_MOCK=true
ENVIRONMENT=development

# Optionnel : forcer les chemins Wikidata si besoin
# WIKIDATA_SERIES_DB_PATH=../wikidata_series_db.json
# WIKIDATA_STANDALONE_CACHE_PATH=../popular_standalone_books.json
